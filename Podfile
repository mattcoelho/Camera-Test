# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Camera Test' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'SwiftyCam'
  pod 'Sharaku'
  pod 'EasyTipView'
  # Pods for Camera Test

  target 'Camera TestTests' do
    inherit! :search_paths
    pod 'SwiftyCam'
    pod 'Sharaku'
    pod 'EasyTipView'
    # Pods for testing
  end

  target 'Camera TestUITests' do
    inherit! :search_paths
    pod 'SwiftyCam'
    pod 'Sharaku'
    pod 'EasyTipView'
    # Pods for testing
  end

end
